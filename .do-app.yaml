name: smart-farmer-backend
region: nyc

services:
  - name: backend
    # Source configuration
    source_dir: backend
    
    # Build configuration
    build_command: pip install --upgrade pip && pip install -r requirements.txt
    
    # Run configuration
    run_command: uvicorn main:app --host 0.0.0.0 --port 8080 --workers 1
    
    # Environment
    environment_slug: python
    
    # Instance configuration
    instance_count: 1
    instance_size_slug: basic-xxs
    
    # HTTP configuration
    http_port: 8080
    
    # Health check
    health_check:
      http_path: /
      initial_delay_seconds: 10
      period_seconds: 10
      timeout_seconds: 3
      success_threshold: 1
      failure_threshold: 3
    
    # Environment variables
    envs:
      - key: PORT
        value: "8080"
      - key: PYTHONUNBUFFERED
        value: "1"
