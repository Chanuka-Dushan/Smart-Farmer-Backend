# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Pydantic (Data Validation)
pydantic==2.5.0
pydantic-settings==2.1.0

# Database (SQLAlchemy + Async Drivers)
sqlalchemy==2.0.23
asyncpg==0.29.0       # <--- ADD THIS (Best Async Postgres driver)
psycopg2-binary==2.9.9 # <--- ADD THIS (Just in case, handles Sync connections)

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Utilities
aiofiles==23.2.1

# Database
databases==0.8.0
aiosqlite==0.19.0

# CORS (built into FastAPI via fastapi.middleware.cors)

# Production server
gunicorn==21.2.0

# REMOVED: fastapi-cors (It is built into FastAPI now)
# REMOVED: databases (SQLAlchemy 2.0+ handles async natively now, 'databases' is often redundant and causes conflicts)